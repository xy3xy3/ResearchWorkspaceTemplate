# Project-local configuration; loaded by Codex only for a trusted project.
# No model pin, no approval bypass, no automatically enabled shell networking.
sandbox_mode = "workspace-write"
approval_policy = "on-request"
web_search = "live"

[agents]
enabled = true
max_concurrent_threads_per_session = 3

[sandbox_workspace_write]
network_access = false
